<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:TrustSync.Desktop.ViewModels.Auth"
             x:Class="TrustSync.Desktop.Views.Auth.FirstRunSetupView"
             x:DataType="vm:FirstRunSetupViewModel">

  <ScrollViewer VerticalScrollBarVisibility="Auto">
    <StackPanel VerticalAlignment="Center" Margin="52,32" Spacing="22" MaxWidth="360">

      <!-- Header -->
      <StackPanel Spacing="8">
        <TextBlock Text="Get Started" FontSize="26" FontWeight="Bold"
                   Foreground="{DynamicResource TextPrimaryBrush}" FontFamily="Trebuchet MS, Verdana, sans-serif"/>
        <TextBlock Text="Create your account to begin managing your finances."
                   FontSize="13.5" Foreground="{DynamicResource TextMutedBrush}" LineHeight="20" TextWrapping="Wrap"
                   FontFamily="Inter"/>
      </StackPanel>

      <!-- Form Fields -->
      <StackPanel Spacing="16">

        <!-- Display Name -->
        <StackPanel Spacing="6">
          <TextBlock Text="Your Name" FontSize="12" FontWeight="SemiBold"
                     Foreground="{DynamicResource TextSecondaryBrush}"/>
          <TextBox Text="{Binding DisplayName}" Watermark="Enter your name"
                   Height="42" FontSize="14" Padding="14,0"
                   VerticalContentAlignment="Center"
                   Background="{DynamicResource BackgroundPrimaryBrush}" Foreground="{DynamicResource TextPrimaryBrush}"
                   BorderBrush="{DynamicResource BorderDefaultBrush}" BorderThickness="1" CornerRadius="10"/>
        </StackPanel>

        <!-- Currency -->
        <StackPanel Spacing="6">
          <TextBlock Text="Default Currency" FontSize="12" FontWeight="SemiBold"
                     Foreground="{DynamicResource TextSecondaryBrush}"/>
          <ComboBox ItemsSource="{Binding AvailableCurrencies}"
                    SelectedItem="{Binding SelectedCurrency}"
                    HorizontalAlignment="Stretch" Height="42"
                    CornerRadius="10" BorderBrush="{DynamicResource BorderDefaultBrush}" BorderThickness="1"
                    Background="{DynamicResource BackgroundPrimaryBrush}"/>
        </StackPanel>

        <!-- Password -->
        <StackPanel Spacing="6">
          <TextBlock Text="Master Password" FontSize="12" FontWeight="SemiBold"
                     Foreground="{DynamicResource TextSecondaryBrush}"/>
          <TextBox PasswordChar="&#x25CF;" Text="{Binding Password}"
                   Watermark="Min 10 chars, upper, lower, digit, special"
                   Height="42" FontSize="14" Padding="14,0"
                   VerticalContentAlignment="Center"
                   Background="{DynamicResource BackgroundPrimaryBrush}" Foreground="{DynamicResource TextPrimaryBrush}"
                   BorderBrush="{DynamicResource BorderDefaultBrush}" BorderThickness="1" CornerRadius="10"/>
        </StackPanel>

        <!-- Confirm Password -->
        <StackPanel Spacing="6">
          <TextBlock Text="Confirm Password" FontSize="12" FontWeight="SemiBold"
                     Foreground="{DynamicResource TextSecondaryBrush}"/>
          <TextBox PasswordChar="&#x25CF;" Text="{Binding ConfirmPassword}"
                   Watermark="Re-enter your password"
                   Height="42" FontSize="14" Padding="14,0"
                   VerticalContentAlignment="Center"
                   Background="{DynamicResource BackgroundPrimaryBrush}" Foreground="{DynamicResource TextPrimaryBrush}"
                   BorderBrush="{DynamicResource BorderDefaultBrush}" BorderThickness="1" CornerRadius="10"/>
        </StackPanel>

      </StackPanel>

      <!-- Validation Errors -->
      <Border IsVisible="{Binding ValidationErrors.Count}"
              Background="{DynamicResource ErrorTintBrush}" CornerRadius="10" Padding="14,10"
              BorderBrush="{DynamicResource DangerBorderBrush}" BorderThickness="1">
        <ItemsControl ItemsSource="{Binding ValidationErrors}">
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <TextBlock Text="{Binding}" Foreground="{DynamicResource ErrorBrush}" FontSize="12"
                         Margin="0,2" TextWrapping="Wrap"/>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
      </Border>

      <!-- Error message -->
      <Border IsVisible="{Binding ErrorMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
              Background="{DynamicResource ErrorTintBrush}" CornerRadius="10" Padding="14,10"
              BorderBrush="{DynamicResource DangerBorderBrush}" BorderThickness="1">
        <TextBlock Text="{Binding ErrorMessage}"
                   Foreground="{DynamicResource ErrorBrush}" FontSize="13" TextWrapping="Wrap"/>
      </Border>

      <!-- Setup button -->
      <Button Command="{Binding SetupCommand}"
              HorizontalAlignment="Stretch" Height="44"
              HorizontalContentAlignment="Center"
              VerticalContentAlignment="Center"
              FontSize="14" FontWeight="SemiBold"
              CornerRadius="10"
              IsEnabled="{Binding !IsBusy}"
              Cursor="Hand"
              BorderThickness="0"
              Background="{DynamicResource AuthBtnGradientBrush}">
        <Panel>
          <TextBlock Text="Create Account" IsVisible="{Binding !IsBusy}"
                     HorizontalAlignment="Center" FontWeight="SemiBold"
                     Foreground="{DynamicResource AuthBtnTextBrush}" FontSize="14"/>
          <StackPanel Orientation="Horizontal" Spacing="8" IsVisible="{Binding IsBusy}"
                      HorizontalAlignment="Center">
            <ProgressBar IsIndeterminate="True" Width="18" Height="18"
                         Foreground="{DynamicResource AuthBtnTextBrush}"/>
            <TextBlock Text="Setting up..." FontWeight="SemiBold" Foreground="{DynamicResource AuthBtnTextBrush}"/>
          </StackPanel>
        </Panel>
      </Button>

    </StackPanel>
  </ScrollViewer>
</UserControl>
